name: smartbandhu_admin
description: SmartBondhu Admin — internal dashboard app (no login)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.8.0
  intl: ^0.20.2
  fl_chart: ^0.70.2
  google_fonts: ^6.2.1
  device_info_plus: ^11.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
